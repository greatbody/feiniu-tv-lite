<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!-- mDNS / NSD 需要 WiFi 多播权限 -->
    <uses-permission android:name="android.permission.CHANGE_WIFI_MULTICAST_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <application
        android:name=".LiteApplication"
        android:allowBackup="true"
        android:icon="@android:drawable/sym_def_app_icon"
        android:label="@string/app_name"
        android:roundIcon="@android:drawable/sym_def_app_icon"
        android:supportsRtl="true"
        android:theme="@style/Theme.FeiniuTvLite"
        android:usesCleartextTraffic="true">

        <activity
            android:name=".modules.splash.SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.FeiniuTvLite">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
                <category android:name="android.intent.category.LEANBACK_LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".modules.server.ScanDeviceActivity"
            android:exported="false"
            android:theme="@style/Theme.FeiniuTvLite" />

        <activity
            android:name=".modules.server.AddDeviceActivity"
            android:exported="false"
            android:theme="@style/Theme.FeiniuTvLite"
            android:windowSoftInputMode="stateHidden|adjustResize" />

        <activity
            android:name=".modules.login.LoginActivity"
            android:exported="false"
            android:theme="@style/Theme.FeiniuTvLite"
            android:windowSoftInputMode="stateHidden|adjustResize" />

        <activity
            android:name=".MainActivity"
            android:exported="false"
            android:theme="@style/Theme.FeiniuTvLite" />

        <activity
            android:name=".modules.detail.DetailActivity"
            android:exported="false"
            android:theme="@style/Theme.FeiniuTvLite" />

        <activity
            android:name=".PlayerActivity"
            android:exported="false"
            android:configChanges="keyboardHidden|orientation|screenSize"
            android:screenOrientation="landscape" />

    </application>

</manifest>
